/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package spotifybackup.app;

import spotifybackup.list.LinkedList;

import static spotifybackup.utilities.StringUtils.join;
import static spotifybackup.utilities.StringUtils.split;
import static spotifybackup.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
